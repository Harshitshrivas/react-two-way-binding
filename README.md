# React Two-Way Binding

This is a simple React concept demonstrating **two-way data binding** using the `useState` hook.  
The input field value and component state stay in sync — when you type, the state updates instantly.

## Key Point
- Demonstrates how `value` and `onChange` work together for two-way binding.
